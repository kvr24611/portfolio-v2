---
const { id = "toast" } = Astro.props;
---

<div id={id} aria-live="polite" class="fixed top-16 right-6 z-50 hidden">
    <div
        class="bg-teal-600 bg-opacity-95 text-white flex items-center gap-2 px-4 py-2 rounded-lg shadow-lg transform opacity-0 translate-x-4 transition-all duration-300 ease-out"
    >
        <!-- icon will be injected via JS -->
        <span data-toast-message
            ><slot>Failed to send. Please try again.</slot></span
        >
    </div>
</div>
